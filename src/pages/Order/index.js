import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Order = () => {
  return (
    <View>
      <Text>Order Page Screen</Text>
    </View>
  );
};

export default Order;

const styles = StyleSheet.create({});